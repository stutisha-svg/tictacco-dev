/**
 * ScoringScreen — placeholder scoring screen (hand-drawn chrome).
 */
import { HandDrawnPlaceholder } from "@/features/HandDrawnPlaceholder";

export function ScoringScreen() {
  return (
    <HandDrawnPlaceholder
      title="Scoring"
      blurb="Match score and round results will show up here."
    />
  );
}
