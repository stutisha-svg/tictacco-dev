/**
 * AchievementsScreen — placeholder achievements gallery (hand-drawn chrome).
 */
import { HandDrawnPlaceholder } from "@/features/HandDrawnPlaceholder";

export function AchievementsScreen() {
  return (
    <HandDrawnPlaceholder
      title="Achievements"
      blurb="Badge collection and progress will land here."
    />
  );
}
