/**
 * `@/features/achievements` — static badge gallery for `/achievements`.
 *
 *   AchievementsScreen    — frame-locked page + invisible scroll + FAB
 *   AchievementsSummary   — cream paper header
 *   AchievementsCard      — sketch card + progress tracker
 *   AchievementsTracker   — ink bar (per card)
 *   AchievementsFabFrame  — sketch fill for back-home FAB
 *   achievementsCatalog   — static gallery data
 */
export { AchievementsScreen } from "./AchievementsScreen";
export { ACHIEVEMENTS_GALLERY } from "./achievementsCatalog";
