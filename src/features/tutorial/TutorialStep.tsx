/**
 * TutorialStep — placeholder tutorial screen (hand-drawn chrome).
 */
import { HandDrawnPlaceholder } from "@/features/HandDrawnPlaceholder";

export function TutorialStep() {
  return (
    <HandDrawnPlaceholder
      title="Tutorial"
      blurb="Walkthrough steps will live here — how to place X/O and win with X-O-X."
    />
  );
}
